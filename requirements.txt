# AURA-AIOSCPU — runtime requirements
#
# Core: no required runtime dependencies — Python 3.10+ stdlib only.
#
# Optional extras (install with pip install -e ".[extras]"):
#   pip install -e ".[metrics]"   # psutil — enhanced CPU/mem metrics
#   pip install -e ".[ai]"        # llama-cpp-python + onnxruntime — local inference
#   pip install -e ".[test]"      # pytest — test suite
#   pip install -e ".[all]"       # all of the above
